## 하네스: async-doctor 개발

**목표:** Node.js/TypeScript 코드의 비동기 순차 처리 병목을 정적 분석으로 탐지하는 CLI 라이브러리 `async-doctor`를 에이전트 팀으로 구축·확장한다.

**트리거:** async-doctor 개발/구축, 새 rule(비동기 패턴 탐지 규칙) 추가, CLI 옵션 추가, reporter 형식 추가, 기존 rule의 오탐 수정 등 관련 작업 요청 시 `async-doctor-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**변경 이력:**

| 날짜       | 변경 내용                                                                                | 대상                                  | 사유                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------- | ---------------------------------------------------------------------------------------- | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-23 | 초기 구성 (4 에이전트 + 5 스킬)                                                          | 전체                                  | 신규 프로젝트, rule 기반 확장 아키텍처에 맞춘 에이전트 팀 구축                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-23 | 초기 구축 실행 완료 (스캐폴드 + rule 2개 + 테스트 30개 + QA)                             | src/, tests/                          | 오케스트레이터 최초 실행. 이 세션에는 `TeamCreate` 도구가 없어 팀 모드 대신 `Agent` 도구 기반 서브 에이전트 순차 실행(core-architect→rule-engineer→test-engineer→qa-reviewer)으로 폴백. `TeamCreate`가 지원되는 세션에서는 오케스트레이터 스킬의 팀 모드를 그대로 사용할 것                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-23 | GitHub(hhw12409/async-doctor) 공개 후 CI에서 `npm ci`가 Node 18/20/22 매트릭스 전부 실패 | .github/workflows/ci.yml, publish.yml | `tsup`(tinyglobby→fdir)와 `ts-morph`(fast-glob→micromatch)가 picomatch에 상충하는 semver 범위를 요구해, npm 버전마다 lockfile 해석이 달라짐. `npm ci`를 `npm install`로 교체해 해결. 로컬에서 `npm ci`가 통과해도 CI의 npm 버전이 다르면 실패할 수 있다는 교훈 — QA 스킬의 "종합 실행" 체크리스트에 로컬 재현만으로 충분하지 않은 사례로 참고                                                                                                                                                                                                                                                                                 |
| 2026-08-23 | v0.2.0 — JSON/SARIF reporter 추가                                                        | src/reporter/, src/cli/index.ts       | v0.1.0에서 "reserved"로 막아둔 `--format json`/`sarif`를 구현. `--format json`은 CI/툴링 연동의 전제조건이고 `--format sarif`는 GitHub Code Scanning에 바로 업로드돼 PR 인라인 주석이 되는, 신생 OSS 정적분석 도구에 레버리지가 가장 큰 기능. `Reporter` 인터페이스와 `REPORTERS` 맵이 애초에 이 확장을 위해 설계돼 있어 위험도 낮음. rule 탐지 정확도 개선(알려진 8종 한계)은 대부분 의도된 false negative이고 새 rule 추가는 실사용 피드백 전 과최적화 위험이 있어 v0.3.0 이후로 미룸. 부수 작업으로 `src/core/package-info.ts`(VERSION/HOMEPAGE를 package.json에서 파생)와 `src/reporter/shared.ts`(경로/카운팅 헬퍼) 신설 |
| 2026-08-27 | v0.3.0 — 신규 rule `no-foreach-async` + 기존 rule FN 4건 수정                            | src/rules/, tests/                    | `array.forEach(async (item) => { await ... })`는 forEach가 콜백의 Promise를 기다리지 않아 에러가 삼켜지거나 완료 순서가 보장되지 않는 흔한 실수 패턴이라 신규 rule로 추가(Array 타입 체크로 Set/Map/NodeList 등 동명이인 오탐 방지). 함께 v0.1.0 QA에서 "의도된 한계"로 미뤄뒀던 8종 FN 중 리스크 낮은 4건(sequential-await: 다중 선언자, 선언 없는 표현식문, 프로퍼티명 텍스트 충돌 — 스코프 인지 의존성 검사로 일반화; no-await-in-loop: do-while)을 수정. 나머지 FN(루프 헤더/조건식 await, 깊이 묻힌 초기화식, 중간 문장 개입)은 오탐 전환 리스크가 높아 이번에도 범위 밖으로 유지. 이 세션도 `TeamCreate` 미지원이라 Agent 기반 순차 실행(rule-engineer→test-engineer→qa-reviewer)으로 진행, core-architect는 타입/인프라 변경이 없어 미소집 — qa-reviewer가 이 판단(diff에 analyzer/cli/core/reporter 무변경)을 별도 검증 |
